# Ollama settings (no API key needed — runs locally)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# Output directory for generated projects (default: ./output)
OUTPUT_DIR=./output

# Optional: log file path (leave blank to log to stdout only)
LOG_FILE_PATH=
